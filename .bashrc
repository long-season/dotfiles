# These are extra changes on top of an already functional bashrc.
# The target .bashrc should be appended, not overwritten.

# Wayland specific changes
# Can also be placed in .bash_profile
export BEMENU_BACKEND=wayland
export XDG_CURRENT_DESKTOP=sway

# Aliases 
# Can also placed in .bash_aliases
# alias docker='podman'

